apiVersion: v1
kind: Service
metadata:
  name: cats-dogs-app
  labels:
    app: cats-dogs-app
spec:
  type: NodePort
  selector:
    app: cats-dogs-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
